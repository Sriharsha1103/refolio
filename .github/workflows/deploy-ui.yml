name: Deploy Refolio UI to GoDaddy

on:
  push:
    branches:
      - prod_new

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Build UI
        working-directory: frontend
        run: npm run build

      - name: Upload build to /refolio
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          # Copy all build artifacts
          source: "refolio/frontend/build/*"
          target: "/var/www/html/refolio"
          # strip_components is not a supported option for appleboy/scp-action.
          # Remove it or replace with 'rm' and 'tar' logic if you need path rewriting.
          # strip_components: 3
